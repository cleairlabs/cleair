name: deploy-production

on:
  push:
    branches: [main]

concurrency:
  group: production-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.2
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}
          script: |
            set -euo pipefail

            export REPO_DIR=/opt/cleair
            export DEPLOY_BRANCH=main
            export REPO_SSH_URL=git@github.com:cleairlabs/cleair.git

            if [ ! -d "${REPO_DIR}/.git" ]; then
              rm -rf "${REPO_DIR}"
              git clone --branch "${DEPLOY_BRANCH}" --single-branch "${REPO_SSH_URL}" "${REPO_DIR}"
            else
              git -C "${REPO_DIR}" remote set-url origin "${REPO_SSH_URL}"
              git -C "${REPO_DIR}" fetch origin "${DEPLOY_BRANCH}"
              git -C "${REPO_DIR}" checkout "${DEPLOY_BRANCH}"
              git -C "${REPO_DIR}" pull --ff-only origin "${DEPLOY_BRANCH}"
            fi

            bash "${REPO_DIR}/scripts/deploy.sh"
